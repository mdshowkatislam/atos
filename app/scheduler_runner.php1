<?php
use Illuminate\Support\Facades\Log;
while (true) {
    Log::info('from app/scheduler_runner.php');  
    // echo "[" . date('Y-m-d H:i:s') . "] Running Laravel schedule...\n";
    // exec('php artisan access:sync'); // this is for auto sync attendance data every minute
    sleep(120);
}